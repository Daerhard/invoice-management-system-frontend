openapi: 3.0.1
info:
  title: Invoice Management System – Complete API
  description: |
    Master specification that combines all services of the Invoice Management System into a single OpenAPI document. The combined openapi.yml is generated from this master file by the Gradle task 'bundleOpenApi'. Do not edit openapi.yml directly; edit the individual service specs instead.
  version: 2.0.0
servers:
  - url: http://localhost:8080/invoice-management-system/api/
    description: local server
paths:
  /v1/customers:
    get:
      tags:
        - customers
      summary: get all customers
      description: returns all customers
      operationId: getAllCustomers
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/customer'
        '400':
          description: invalid user id supplied in authentication token
        '401':
          description: not authenticated
  /v1/customers/professional:
    get:
      tags:
        - customers
      summary: get all professional customers
      description: returns all customers where isProfessional is true
      operationId: getProfessionalCustomers
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/customer'
        '400':
          description: invalid user id supplied in authentication token
        '401':
          description: not authenticated
  /v1/customers/{userName}/email:
    patch:
      tags:
        - customers
      summary: update customer email
      description: updates the email address of a specific customer
      operationId: updateCustomerEmail
      parameters:
        - name: userName
          in: path
          required: true
          description: The username of the customer.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/emailUpdate'
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer'
        '400':
          description: invalid input
        '404':
          description: customer not found
  /v1/import:
    post:
      tags:
        - CSVImport
      summary: Upload a CSV file for database import
      description: Accepts a CSV file and imports its data into the database.
      operationId: importCSVData
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: The CSV file to be uploaded.
      responses:
        '201':
          description: CSV file imported successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '400':
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '500':
          description: Server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/orders:
    get:
      tags:
        - orders
      summary: get all orders
      description: returns all orders
      operationId: getOrders
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/cardmarketOrder'
        '400':
          description: invalid user id supplied in authentication token
        '401':
          description: not authenticated
  /v1/orders/{userName}:
    get:
      tags:
        - orders
      summary: get all orders by user name
      description: returns all orders by user name
      operationId: getOrdersByUserName
      parameters:
        - name: userName
          in: path
          required: true
          description: The username for which to retrieve orders.
          schema:
            type: string
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/cardmarketOrder'
        '400':
          description: invalid user id supplied in authentication token
        '401':
          description: not authenticated
  /v1/orders/{externalOrderId}/invoice:
    post:
      tags:
        - orders
      summary: Create and persist an invoice for an order
      description: Generates a PDF invoice for the given order, persists it in the database, and returns the invoice data. Returns 409 if an invoice already exists for this order.
      operationId: createInvoice
      parameters:
        - name: externalOrderId
          in: path
          required: true
          description: The external order id.
          schema:
            type: integer
            format: int64
      responses:
        '201':
          description: Invoice created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invoice'
        '404':
          description: Order not found.
        '409':
          description: Invoice already exists for this order.
  /v1/users:
    post:
      tags:
        - users
      summary: Create a new user
      description: Persists a new user in the database and returns the created user.
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/user'
      responses:
        '201':
          description: User created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/user'
        '400':
          description: Invalid input.
    get:
      tags:
        - users
      summary: Get all users
      description: Returns a list of all users.
      operationId: getAllUsers
      responses:
        '200':
          description: Successful operation.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/user'
  /v1/users/{id}:
    get:
      tags:
        - users
      summary: Get user by id
      description: Returns a single user by its id.
      operationId: getUserById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/user'
        '404':
          description: User not found.
  /v1/generateInvoicePDF/{cardmarketExternalOrderId}:
    get:
      tags:
        - InvoiceGenerationPDF
      summary: Generate and save an invoice in pdf format
      description: Generate an invoice in pdf format and returns it for saving on the client's machine.
      operationId: getInvoicePDF
      parameters:
        - name: cardmarketExternalOrderId
          in: path
          required: true
          description: The external order id.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: PDF file generated successfully.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '500':
          description: Server error.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Internal server error occurred while generating the PDF.
  /v1/generateInvoicesPDF/{startDate}/{endDate}:
    get:
      tags:
        - InvoiceGenerationPDF
      summary: Download invoices as a ZIP file
      description: Generates multiple invoices in PDF format and compresses them into a ZIP file.
      operationId: getInvoicesPDF
      parameters:
        - name: startDate
          in: path
          required: true
          schema:
            type: string
            format: date
          description: Start date of the period (YYYY-MM-DD)
        - name: endDate
          in: path
          required: true
          schema:
            type: string
            format: date
          description: End date of the period (YYYY-MM-DD)
      responses:
        '200':
          description: ZIP file containing invoices
          content:
            application/zip:
              schema:
                type: string
                format: byte
  /v1/generateInvoiceZUGFeRD/{cardmarketExternalOrderId}:
    get:
      tags:
        - InvoiceGenerationZUGFeRD
      summary: Generate and save an invoice in ZUGFeRD format
      description: Generate an invoice in ZUGFeRD format and returns it for saving on the client's machine.
      operationId: getInvoiceZUGFeRD
      parameters:
        - name: cardmarketExternalOrderId
          in: path
          required: true
          description: The external order id.
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: ZUGFeRD file generated successfully.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '500':
          description: Server error.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Internal server error occurred while generating the Invoice.
  /v1/purchase-invoices:
    post:
      tags:
        - PurchaseInvoices
      summary: Create a new purchase invoice
      description: Creates a new purchase invoice with a product name. Items can be added separately.
      operationId: createPurchaseInvoice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/purchaseInvoice'
      responses:
        '201':
          description: Purchase invoice created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/purchaseInvoice'
        '400':
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
    get:
      tags:
        - PurchaseInvoices
      summary: Get all purchase invoices
      operationId: getAllPurchaseInvoices
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/purchaseInvoice'
  /v1/purchase-invoices/{id}/items:
    post:
      tags:
        - PurchaseInvoices
      summary: Add an item to a purchase invoice
      description: Adds a new purchase invoice item (with an optional PDF document) to the given purchase invoice.
      operationId: addPurchaseInvoiceItem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                itemData:
                  $ref: '#/components/schemas/purchaseInvoiceItem'
                pdf:
                  type: string
                  format: binary
              required:
                - itemData
      responses:
        '201':
          description: Item added successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/purchaseInvoice'
        '400':
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '404':
          description: Purchase invoice not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '500':
          description: Server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/purchase-invoices/{id}/items/{itemId}:
    delete:
      tags:
        - PurchaseInvoices
      summary: Remove an item from a purchase invoice
      description: Removes a purchase invoice item from the given purchase invoice. Returns 404 if the invoice or item is not found, or 409 if the item does not belong to the given invoice.
      operationId: deletePurchaseInvoiceItem
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the purchase invoice.
          schema:
            type: integer
            format: int32
        - name: itemId
          in: path
          required: true
          description: The id of the purchase invoice item to remove.
          schema:
            type: integer
            format: int32
      responses:
        '204':
          description: Purchase invoice item removed successfully.
        '404':
          description: Purchase invoice or item not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '409':
          description: Item does not belong to the given purchase invoice.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/purchase-invoices/{id}/items/{itemId}/pdf:
    get:
      tags:
        - PurchaseInvoices
      summary: Download the PDF document of a purchase invoice item
      operationId: getPurchaseInvoiceItemPdf
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: PDF file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          description: PDF not found
  /v1/refunds:
    post:
      tags:
        - Refunds
      summary: Add a refund
      description: Creates a new refund entry with a money value and year.
      operationId: createRefund
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refund'
      responses:
        '201':
          description: Refund created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refund'
        '400':
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
    get:
      tags:
        - Refunds
      summary: Get all refunds
      operationId: getAllRefunds
      responses:
        '200':
          description: Successful operation.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/refund'
  /v1/refunds/{id}:
    delete:
      tags:
        - Refunds
      summary: Remove a refund
      description: Deletes the refund with the given id.
      operationId: deleteRefund
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the refund.
          schema:
            type: integer
            format: int32
      responses:
        '204':
          description: Refund removed successfully.
        '404':
          description: Refund not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/supplies:
    post:
      tags:
        - Supplies
      summary: Add a supply
      description: Creates a new supply entry with an optional PDF document, a money value, a date, and a product name.
      operationId: createSupply
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                supplyData:
                  $ref: '#/components/schemas/supply'
                pdf:
                  type: string
                  format: binary
              required:
                - supplyData
      responses:
        '201':
          description: Supply created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/supply'
        '400':
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '500':
          description: Server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
    get:
      tags:
        - Supplies
      summary: Get all supplies
      operationId: getAllSupplies
      responses:
        '200':
          description: Successful operation.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/supply'
  /v1/supplies/{id}:
    delete:
      tags:
        - Supplies
      summary: Remove a supply
      description: Deletes the supply with the given id.
      operationId: deleteSupply
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the supply.
          schema:
            type: integer
            format: int32
      responses:
        '204':
          description: Supply removed successfully.
        '404':
          description: Supply not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/supplies/{id}/pdf:
    get:
      tags:
        - Supplies
      summary: Download the PDF document of a supply
      operationId: getSupplyPdf
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: PDF file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          description: Supply or document not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/purchases/import:
    post:
      tags:
        - cardmarketPurchases
      summary: Upload a CSV file for cardmarket purchases import
      description: Accepts a CSV file and imports cardmarket purchase data into the database.
      operationId: importCSVPurchaseData
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: The CSV file to be uploaded.
      responses:
        '201':
          description: CSV file imported successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '400':
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
        '500':
          description: Server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/responseMessage'
  /v1/purchases:
    get:
      tags:
        - cardmarketPurchases
      summary: Get all cardmarket purchases
      description: Returns all cardmarket purchases with their items.
      operationId: getPurchases
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/cardmarketPurchase'
  /v1/purchases/{userName}:
    get:
      tags:
        - cardmarketPurchases
      summary: Get cardmarket purchases by seller username
      description: Returns all cardmarket purchases from the given seller username.
      operationId: getPurchasesByUserName
      parameters:
        - name: userName
          in: path
          required: true
          description: The username of the seller.
          schema:
            type: string
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/cardmarketPurchase'
  /v1/email/send:
    post:
      tags:
        - email
      summary: Send an email
      description: Sends a plain-text or HTML email to the specified recipient.
      operationId: sendEmail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/emailSendRequest'
      responses:
        '200':
          description: Email sent successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/emailSendResponse'
        '400':
          description: Invalid request data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/emailSendResponse'
        '500':
          description: Failed to send email.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/emailSendResponse'
  /v1/email/invoice/{orderId}:
    post:
      tags:
        - email
      summary: Send invoice email
      description: |
        Generates the PDF invoice for the given order and sends it to the customer's stored email address. The recipient address can be overridden via the optional request body. Returns 404 when the order does not exist or the customer has no email address and no override was provided.
      operationId: sendInvoiceEmail
      parameters:
        - name: orderId
          in: path
          required: true
          description: External order id of the order whose invoice should be emailed.
          schema:
            type: integer
            format: int64
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/invoiceEmailRequest'
      responses:
        '200':
          description: Invoice email sent successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/emailSendResponse'
        '404':
          description: Order not found or customer has no email address.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/emailSendResponse'
        '500':
          description: Failed to generate or send the invoice email.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/emailSendResponse'
components:
  schemas:
    user:
      type: object
      description: user object
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the user.
        username:
          type: string
          description: Username of the user.
        password:
          type: string
          description: Password of the user.
          writeOnly: true
          nullable: true
        firstName:
          type: string
          description: First name of the user.
        lastName:
          type: string
          description: Last name of the user.
        zipCode:
          type: string
          description: ZIP code of the user's address.
        city:
          type: string
          description: City of the user's address.
        street:
          type: string
          description: Street of the user's address.
        email:
          type: string
          description: Email address of the user.
      required:
        - username
        - firstName
        - lastName
        - zipCode
        - city
        - street
        - email
    customer:
      type: object
      description: customer object
      properties:
        user_name:
          type: string
          description: user name of the customer
          example: FakeJohnny
        is_professional:
          type: boolean
          description: status of the customer
          example: true
        email:
          type: string
          nullable: true
          description: email address of the customer
          example: customer@example.com
      required:
        - user_name
        - is_professional
    emailUpdate:
      type: object
      description: request body for updating customer email
      properties:
        email:
          type: string
          nullable: true
          description: new email address of the customer
          example: customer@example.com
      required:
        - email
    cardmarketOrder:
      type: object
      description: order object
      properties:
        customer:
          $ref: '#/components/schemas/customer'
        order_id:
          type: integer
          format: int64
          description: Unique identifier for the order.
        payment_date:
          type: string
          format: date
          description: Date of payment for the order.
        article_count:
          type: integer
          format: int32
          description: Number of articles in the order.
        merchandise_value:
          type: number
          format: double
          description: Total value of the merchandise in the order.
        shipment_cost:
          type: number
          format: double
          description: Cost of shipping for the order.
        total_value:
          type: number
          format: double
          description: Total value of the order, including merchandise and shipment costs.
        commission:
          type: number
          format: double
          description: Commission charged for the order.
        currency:
          type: string
          description: Currency code for monetary values.
          example: EUR
        orderItems:
          type: array
          description: List of items associated with the purchase.
          items:
            $ref: '#/components/schemas/orderItem'
        invoice:
          $ref: '#/components/schemas/invoice'
          nullable: true
      required:
        - customer
        - order_id
        - payment_date
        - article_count
        - merchandise_value
        - shipment_cost
        - total_value
        - commission
        - currency
    orderItem:
      type: object
      description: Order item object
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the order item.
        orderId:
          type: integer
          format: int64
          description: Identifier of the associated purchase.
        count:
          type: integer
          format: int32
          description: Number of items in the order.
        condition:
          type: string
          description: Condition of the item (e.g., new, used).
        price:
          type: number
          format: double
          description: Price of the individual item.
        card:
          $ref: '#/components/schemas/card'
      required:
        - order_id
        - count
        - condition
        - price
        - card
    card:
      type: object
      description: Card object
      properties:
        id:
          $ref: '#/components/schemas/cardId'
        complete_description:
          type: string
          description: Detailed description of the card.
        product_name:
          type: string
          description: Name of the product associated with the card.
        name:
          type: string
          description: Name of the card.
        language:
          type: string
          description: Language of the card.
        rarity:
          type: string
          description: Rarity level of the card.
        product_id:
          type: integer
          format: int64
          description: Identifier of the associated product.
      required:
        - id
        - product_name
        - name
        - language
        - rarity
        - product_id
    cardId:
      type: object
      description: Composite key for the Card entity.
      properties:
        konamiSet:
          type: string
          description: Konami set identifier of the card.
        number:
          type: string
          description: Unique card number within the Konami set.
      required:
        - konamiSet
        - number
    invoice:
      type: object
      description: Invoice associated with an order.
      properties:
        id:
          type: integer
          format: int64
          description: Unique identifier for the invoice.
        orderId:
          type: integer
          format: int64
          description: External order id this invoice belongs to.
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the invoice was created.
        invoicePdf:
          type: string
          format: byte
          nullable: true
          description: PDF content of the invoice as a base64-encoded byte array.
        sent:
          type: boolean
          description: Indicates whether the invoice has been sent to the customer.
      required:
        - id
        - orderId
        - createdAt
        - sent
    responseMessage:
      type: object
      properties:
        message:
          type: string
          example: CSV schema validation failed. / CSV file import was successful. / Internal Server Error.
    purchaseInvoice:
      type: object
      description: A purchase invoice containing a product name, a computed total price, and a list of items.
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the purchase invoice.
        productName:
          type: string
          description: Name of the purchased product.
        totalPrice:
          type: number
          format: double
          description: Total price computed from all items.
          readOnly: true
        items:
          type: array
          description: List of purchase invoice items.
          readOnly: true
          items:
            $ref: '#/components/schemas/purchaseInvoiceItem'
      required:
        - productName
    purchaseInvoiceItem:
      type: object
      description: A single item entry within a purchase invoice.
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the purchase invoice item.
        purchaseType:
          type: string
          enum:
            - DISPLAY
            - CASE
            - BOOSTER
          description: Type of item purchased.
        amount:
          type: integer
          format: int32
          description: Quantity purchased.
        price:
          type: number
          format: double
          description: Price per unit.
        invoiceDate:
          type: string
          format: date
          description: Date of the invoice.
      required:
        - purchaseType
        - amount
        - price
        - invoiceDate
    refund:
      type: object
      description: A refund entry with a money value and a year.
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the refund.
        value:
          type: number
          format: double
          description: The refunded money value.
        year:
          type: integer
          format: int32
          description: The year of the refund.
      required:
        - value
        - year
    supply:
      type: object
      description: A supply entry with an optional PDF document, a money value, a date, and a product name.
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the supply.
        value:
          type: number
          format: double
          description: The money value of the supply.
        supplyDate:
          type: string
          format: date
          description: The date of the supply.
        product:
          type: string
          description: The name of the supplied product.
      required:
        - value
        - supplyDate
        - product
    cardmarketPurchase:
      type: object
      description: Cardmarket purchase object representing a purchase made by the user.
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the purchase.
        sellerUserName:
          type: string
          description: Username of the seller on Cardmarket.
        externalOrderId:
          type: integer
          format: int64
          description: External order ID from Cardmarket.
        dateOfPayment:
          type: string
          format: date
          description: Date of payment.
        articleCount:
          type: integer
          format: int32
          description: Number of articles in the purchase.
        merchandiseValue:
          type: number
          format: double
          description: Total merchandise value.
        shipmentCost:
          type: number
          format: double
          description: Shipment cost.
        trusteeFee:
          type: number
          format: double
          description: Trustee service fee charged by Cardmarket.
        totalValue:
          type: number
          format: double
          description: Total value including all costs.
        currency:
          type: string
          description: Currency code.
          example: EUR
        purchaseItems:
          type: array
          description: List of items in the purchase.
          items:
            $ref: '#/components/schemas/purchaseItem'
      required:
        - sellerUserName
        - externalOrderId
        - dateOfPayment
        - articleCount
        - merchandiseValue
        - shipmentCost
        - trusteeFee
        - totalValue
        - currency
    purchaseItem:
      type: object
      description: A single item in a cardmarket purchase.
      properties:
        id:
          type: integer
          format: int32
          description: Unique identifier for the purchase item.
        purchaseId:
          type: integer
          format: int32
          description: Identifier of the associated purchase.
        count:
          type: integer
          format: int32
          description: Number of cards in this item.
        condition:
          type: string
          description: Condition of the card (e.g., NM, EX).
        price:
          type: number
          format: double
          description: Price per card.
        isFirstEdition:
          type: boolean
          description: Whether the card is a first edition.
        card:
          $ref: '#/components/schemas/card'
      required:
        - count
        - condition
        - price
        - isFirstEdition
        - card
    emailSendRequest:
      type: object
      description: Request body for sending a generic email.
      properties:
        to:
          type: string
          description: Recipient email address.
          example: customer@example.com
        subject:
          type: string
          description: Subject line of the email.
          example: Your invoice
        body:
          type: string
          description: |
            Body content of the email. Treated as plain text when isHtml is false, as HTML when isHtml is true.
          example: Dear customer, please find your invoice attached.
        isHtml:
          type: boolean
          description: When true the body is treated as HTML.
          default: false
      required:
        - to
        - subject
        - body
    invoiceEmailRequest:
      type: object
      description: |
        Optional request body for the invoice email endpoint. All fields are optional; omitting the body entirely is valid.
      properties:
        to:
          type: string
          nullable: true
          description: |
            Override recipient email address. When omitted the customer's stored email is used.
          example: customer@example.com
        subject:
          type: string
          nullable: true
          description: |
            Override subject line. When omitted a default subject is generated from the order details.
          example: 'Your invoice #12345'
    emailSendResponse:
      type: object
      description: Response returned after an email send attempt.
      properties:
        message:
          type: string
          example: Email sent successfully.
